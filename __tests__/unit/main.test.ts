import { describe, it, afterEach, beforeEach } from 'vitest';

describe('greeter function', () => {
  beforeEach(() => {
  });

  afterEach(() => {
  });

  it('outputs hello', async () => {
      console.log("hello");
  });
});
